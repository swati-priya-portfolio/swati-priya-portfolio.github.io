name: Origin Story Transform
on:
  push:
    branches:
      - fix/origin-story-motion-20260825
    paths:
      - tools/origin_story_transform.py
      - .github/workflows/origin-story-transform.yml
jobs:
  tune:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - run: python tools/origin_story_transform.py
      - run: |
          git config user.name "ChatGPT Motion Repair"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add motion.js motion.css
          if git diff --cached --quiet; then
            echo "No changes"
          else
            git commit -m "Transform Origin story from messy thoughts to clarity"
            git push
          fi
